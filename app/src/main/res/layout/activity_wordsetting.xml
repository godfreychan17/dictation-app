<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/viewBGColor"
    android:id="@+id/wordSettingRootView"
    >
    
    <RelativeLayout
        android:id="@+id/wordSettingTopBar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/topBarHeight"
        android:background="@color/white"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/wordSettingActivityTitle"
            />
    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/wordSettingRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/wordSettingTopBar"
        app:layout_constraintBottom_toTopOf="@id/wordSettingBottomBar"/>

    <RelativeLayout
        android:id="@+id/wordSettingBottomBar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottomBarHeight"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@color/bottomBarBGColor"
        >

        <Button
            android:id="@+id/wordSettingConfirmBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/confirmBtnTitle"
            android:layout_centerInParent="true"
            />
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>